server:
  port: 8080
spring:
  application:
    name: dashboard-service
services:
  invoice:
    base-url: http://localhost:8081
  market:
    base-url: http://localhost:8082
jwt:
  secret: ${JWT_SECRET:change-this-super-long-jwt-secret-change-this-super-long-jwt-secret}
